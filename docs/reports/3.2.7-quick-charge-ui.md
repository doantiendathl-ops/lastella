# Report: 3.2.7 Quick-Charge UI

**Status:** Complete  
**Branch:** phase-3  
**Date:** 2026-07-02

## Summary

Updated the folio charge UI to support catalog tile selection, stay assignment dropdown, and posting-source badge display.

## Changes

### Controller (`BookingController::show()`)
- Added `serviceRates` (filtered to bookable rates), `checkableStays` (CheckedIn stays for the booking), and `currentBusinessDate` to the Inertia props.

### Vue Components
- **`Show.vue`**: Passed `serviceRates` and `checkableStays` down to `FolioPanel`.
- **`FolioPanel.vue`**: Forwarded service rates and checkable stays to `AddChargeForm` and `FolioEntryTable`.
- **`AddChargeForm.vue`**: Full rewrite — catalog tile picker toggles pre-fill; stay dropdown assigns `stay_id`; preview amount computed from `quantity × unit_price`; ROOM type excluded from manual charge_type list.
- **`FolioEntryTable.vue`**: Added "Phòng" (room number) and "Nguồn" (posting source badge) columns; badge colours: NIGHT_AUDIT=indigo, SYSTEM_AUTO=amber, MANUAL=gray.

## Test Results
- 0 new failures introduced (baseline 50 → 50 after Task #8)
